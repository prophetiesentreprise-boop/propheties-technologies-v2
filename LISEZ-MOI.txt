CORRECTIF — Images cassées + lien admin

1. client/src/data/siteContent.ts
   Remplace le fichier à ce chemin exact dans ton projet.
   (Toutes les images /manus-storage/... cassées ont été remplacées
   par de vraies images qui fonctionnent, et le logo pointe maintenant
   vers un fichier local fiable.)

2. client/public/propheties-logo.png
   Nouveau fichier à ajouter à ce chemin exact (créer le dossier public/
   s'il n'existe pas déjà dans client/).

3. scripts/create-owner.ts
   Remplace le fichier existant (corrige l'URL affichée : /admin/connexion
   au lieu de /admin/login, pour les prochaines fois où tu créeras un compte).

Ensuite : GitHub Desktop > Commit > Push. Vercel redéploiera automatiquement.

Pour le lien admin qui donne une 404 avec ?entry=..., c'est un souci séparé,
encore en cours de diagnostic — pas résolu par ce correctif-ci.
